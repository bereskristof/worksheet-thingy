<UserControl x:Class="Interface.Password.PasswordPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:res="clr-namespace:Interface.Resources"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Column="1" Grid.Row="1" Background="{DynamicResource {x:Static SystemColors.MenuBrushKey}}"
                BorderBrush="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey}}"
                BorderThickness="1,1,1,1" Padding="4, 4, 4, 4" MinWidth="200">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label Content="{x:Static res:Lang.Password_Label}" HorizontalAlignment="Left" Padding="0, 0, 0, 4"/>
                <PasswordBox Grid.Row="1" Name="MainPasswordBox" PasswordChanged="MainPasswordBox_OnPasswordChanged"/>
                <Button Grid.Row="2" Content="{x:Static res:Lang.Password_Enter}" Margin="0, 2, 0, 0" HorizontalAlignment="Right" Padding="4, 0" IsDefault="True" Click="ButtonBase_OnClick"/>
            </Grid>
        </Border>
        <!-- Since it loads too fast for the indeterminate bar, I just set an arbitrary value. -->
        <ProgressBar Grid.Column="1" Grid.Row="2" Name="LoadingBar" Value="70" Visibility="Hidden" Height="10" VerticalAlignment="Top" Margin="0, 4"/>
        <Label Grid.Column="1" Grid.Row="2" Content="{x:Static res:Lang.Password_Failure}" Name="IncorrectLabel" Visibility="Hidden"/>
    </Grid>
</UserControl>

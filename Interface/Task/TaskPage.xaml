<UserControl x:Class="Interface.Task.TaskPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:res="clr-namespace:Interface.Resources"
             xmlns:storage="clr-namespace:Storage.Task;assembly=Storage"
             xmlns:math="clr-namespace:WpfMath.Controls;assembly=WpfMath"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <DataGrid Grid.Row="0" Name="TaskList" ItemsSource="{Binding}" AutoGenerateColumns="False" IsReadOnly="True"
                      SelectionChanged="TaskList_OnSelectionChanged" GridLinesVisibility="None" SelectionMode="Single" HorizontalScrollBarVisibility="Disabled" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding (storage:Question.Id)}" Width="30"/>
                    <DataGridTextColumn Header="Question" Binding="{Binding (storage:Question.Text)}" Width="*"/>
                    <DataGridTextColumn Header="Answers" Binding="{Binding (storage:Question.AnswerCount)}" Width="55"/>
                </DataGrid.Columns>
            </DataGrid>
            <Button Grid.Row="1" Content="{x:Static res:Lang.Task_Add}" Click="ButtonBase_OnClick"/>
        </Grid>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="5" Margin="3,0,0,0"/>
        <Grid Grid.Column="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="72"/>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <math:FormulaControl Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="0" Formula="23 + \frac{9}{99}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <GridSplitter Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1" VerticalAlignment="Stretch" Height="5" HorizontalAlignment="Stretch"/>
            <TextBlock Grid.Column="0" Grid.Row="2" Text="{x:Static res:Lang.Task_Question}" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="0,0,6,0"/>
            <TextBox Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2" TextWrapping="WrapWithOverflow" AcceptsReturn="True" Name="QuestionBox" Text="{Binding Path=.}"/>
            <TextBlock Grid.Column="0" Grid.Row="3" Text="{x:Static res:Lang.Task_Score}" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="0,0,6,0" Margin="0,3,0,0"/>
            <TextBox Grid.Column="1" Grid.Row="3" Margin="0,3,0,0" Name="ScoreBox"/>
            <TextBlock Grid.Column="2" Grid.Row="3" Margin="3,3,0,0" Text="LqScoreError" Visibility="Hidden" Name="ScoreErrorText"/>
            <TextBlock Grid.Column="0" Grid.Row="4" Text="{x:Static res:Lang.Task_Answers}" HorizontalAlignment="Right" VerticalAlignment="Top" Padding="0,0,6,0" Margin="0,3,0,0"/>
            <StackPanel Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="4" Name="AnswerListPanel"/>
            <Button Grid.Column="2" Grid.Row="5" HorizontalAlignment="Right" Content="{x:Static res:Lang.Task_AddAnswer}" Margin="0,3,0,0" Padding="4,1,4,1" Click="ButtonAddAnswer_OnClick"/>
        </Grid>
    </Grid>
</UserControl>

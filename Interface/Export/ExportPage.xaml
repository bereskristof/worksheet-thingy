<UserControl x:Class="Interface.Export.ExportPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Content="MakeIt!" VerticalAlignment="Top"/>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="5" Margin="3,0,3,0" />
        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" BorderBrush="{DynamicResource {x:Static SystemColors.ScrollBarBrushKey}}" BorderThickness="1">
                <ScrollViewer Background="{DynamicResource {x:Static SystemColors.MenuBrushKey}}"
                              HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Canvas Name="PreviewScroll"/>
                </ScrollViewer>
            </Border>
            <ProgressBar Grid.Row="1" Height="10" IsIndeterminate="True" Name="PreviewProgressBar" Visibility="Collapsed" Margin="0,3,0,0"/>
            <Grid Grid.Row="2" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Name="CreateButton" Content="Create preview" Click="ButtonBase_OnClick" Margin="0, 3, 3, 0" Padding="3, 0"/>
                <Button Grid.Column="1" Content="Zoom out" Click="ZoomOut_OnClick" Margin="0, 3, 3, 0" Padding="3, 0"/>
                <Button Grid.Column="2" Content="Zoom in" Click="ZoomIn_OnClick" Margin="0, 3, 3, 0" Padding="3, 0"/>
                <ComboBox Grid.Column="3" HorizontalAlignment="Right"  Margin="3, 3, 0, 0" Padding="3, 0">
                    <ComboBoxItem Content="Large" Selected="ScaleLarge_OnSelect"/>
                    <ComboBoxItem Content="Medium" IsSelected="True" Selected="ScaleMedium_OnSelect"/>
                    <ComboBoxItem Content="Small" Selected="ScaleSmall_OnSelect"/>
                </ComboBox>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
